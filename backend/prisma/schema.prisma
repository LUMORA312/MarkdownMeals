generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../dev.db"
}

model Restaurant {
  id          String   @id @default(cuid())
  name        String
  coverImage  String
  redirectUrl String
  rating      Float?
  createdAt   DateTime @default(now())

  categories RestaurantCategory[]
  dishes     Dish[]
  tokens     RedirectToken[]
  ratings    Rating[]
  favorites  Favorite[]
}

model RestaurantCategory {
  id           String     @id @default(cuid())
  name         String
  restaurantId String
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)

  @@index([restaurantId])
}

model Dish {
  id            String   @id @default(cuid())
  name          String
  image         String
  category      String
  primaryTaste  String
  price         Float?
  originalPrice Float?
  dealType      String?
  dealExpiresAt DateTime?
  restaurantId  String
  restaurant    Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)

  modifiers DishModifier[]
  tokenDishes TokenDish[]

  @@index([restaurantId])
}

model DishModifier {
  id     String @id @default(cuid())
  name   String
  dishId String
  dish   Dish   @relation(fields: [dishId], references: [id], onDelete: Cascade)

  @@index([dishId])
}

model RedirectToken {
  id                 String   @id @default(cuid())
  restaurantId       String
  restaurant         Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  createdAt          DateTime @default(now())
  ratingAvailableAt  DateTime

  dishes TokenDish[]
  rating Rating?

  @@index([restaurantId])
}

model TokenDish {
  id      String @id @default(cuid())
  tokenId String
  dishId  String
  token   RedirectToken @relation(fields: [tokenId], references: [id], onDelete: Cascade)
  dish    Dish          @relation(fields: [dishId], references: [id], onDelete: Cascade)

  @@unique([tokenId, dishId])
  @@index([tokenId])
  @@index([dishId])
}

model Rating {
  id        String   @id @default(cuid())
  emoji     String
  tokenId   String   @unique
  token     RedirectToken @relation(fields: [tokenId], references: [id], onDelete: Cascade)
  restaurantId String
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  tags RatingTag[]

  @@index([restaurantId])
}

model RatingTag {
  id       String @id @default(cuid())
  name     String
  ratingId String
  rating   Rating @relation(fields: [ratingId], references: [id], onDelete: Cascade)

  @@index([ratingId])
}

model Favorite {
  id           String     @id @default(cuid())
  sessionId    String
  restaurantId String
  restaurant   Restaurant @relation(fields: [restaurantId], references: [id], onDelete: Cascade)
  createdAt    DateTime   @default(now())

  @@unique([sessionId, restaurantId])
  @@index([sessionId])
  @@index([restaurantId])
}
