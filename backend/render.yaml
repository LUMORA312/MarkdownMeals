services:
  - type: web
    name: foodman-api
    runtime: node
    rootDir: backend
    buildCommand: yarn install && yarn build
    startCommand: yarn start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
